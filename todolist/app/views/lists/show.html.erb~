<p id="notice"><%= notice %></p>

<div class="form-group">
  <strong>Tytuł:</strong>
  <%= @list.title %>
</div>

<div class="form-group">
  <strong>Zawartość:</strong>
  <%= @list.content %>
</div>

<div class="form-group">
  <strong>Data:</strong>
  <%= @list.date %>
</div>

<form class="form-inline">
	<div class="actions">
		<%= link_to 'Powrót', lists_path, { :class=>"btn btn-default" } %>
	  <%= link_to 'Edytuj', edit_list_path(@list), :class => 'btn btn-primary' %>
	</div>
</form>
